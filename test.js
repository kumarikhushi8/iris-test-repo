// A deliberately passing test to start with — we'll break this on demand later
// retest with PR lookup fix
const assert = require("assert");
assert.strictEqual(1 + 1, 19);
console.log("test passed");
